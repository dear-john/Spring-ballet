#include<iostream>
#include<string.h>
using namespace std;
const int N=1005;
int main()
{
	char old[N];
	while(gets(old))//使用gets()可以读取除enter之外的符号 
	{
		int len=strlen(old);
		for(int i=0;i<len;i++)
		{
			if(old[i]>=65&&old[i]<=90)
				old[i]+=32;
		}
		cout<<old<<endl;
	}
	return 0;
} 
